#include <stdio.h>
#include <stdlib.h>

int main() {

  char s1[256];
  char s2256] = "Imagine";
  char s[] = "Basketball";
  int sLen = 0;
  
  while(s[sLen] != 0) {
    sLen ++;
  }

  printf("length of s = &d\n", sLen);

  return 0;
}
